# Coffee Cellar 開発ロードマップ

作成日：2026-09-17  
参照仕様：`coffee_cellar_codex_handoff.md`

## 進捗（2026-09-17）

画面遷移、データ契約、日付計算、バックアップとPWA更新の設計を[DESIGN.md](DESIGN.md)に具体化した。段階0の設計・Git初期化と、段階1の在庫管理を実装済み。追加・一覧・詳細・編集・経過期間・古い順表示・アーカイブ・確認付き削除を利用できる。Nodeテスト16件、Chromeブラウザテスト6件が成功し、390px幅の画面を確認した。iPhone実機検証は未着手。起動方法は[README.md](README.md)を参照。

段階1で予定していたPWA最小接続の確認は、キャッシュによる開発中の旧版混在を避けるため段階3へまとめる。現時点ではオフライン起動に対応していない。写真のメニュー9種類をIndexedDBの初期プリセットとして追加し、追加・編集フォームから選択可能にした（DB v2へ移行、既存の豆は保持）。段階2のプリセット管理・期間フィルター・JSONバックアップと復元も実装済み。書き出しと復元の往復一致、復元途中のロールバック、空データ復元、不正ファイル拒否を検証した。次は段階3のCSV・PWAを実装する。

## 目的と今回の範囲

iPhoneから、ワインセラーで長期保管しているコーヒー豆を把握する個人用Webアプリを作る。中心となる体験は「何が残っているか」「焙煎からどれだけ経ったか」をすぐ確認できること。

今回は仕様の確認と開発計画の整理までとする。添付仕様書の第25章にある実装指示は将来の実装要件として扱い、この段階ではアプリの作成・公開は行わない。確認時点の作業フォルダは空で、既存コードやGitリポジトリはない。

## 基本方針

- アプリ名は **Coffee Cellar**、ホーム画面用の短縮名は **Coffee**。
- HTML / CSS / Vanilla JavaScriptを第一候補にし、配信時のビルド工程を不要にする。
- GitHub Pagesで静的配信し、IndexedDBに在庫・履歴・プリセットを保存する。
- Service WorkerとmanifestでPWA化し、初回の読み込みとキャッシュ完了後はオフラインで使えるようにする。
- iPhoneの片手操作を中心に、下部ナビゲーション、大きめのタップ領域、safe areaへの対応を設計する。
- JSONバックアップを初期版の必須機能とし、実データで試用する前に復元まで確認する。
- ログイン、サーバーDB、クラウド同期、写真、通知、AI、重量・金額管理は初期版に含めない。

技術構成は添付仕様に沿った採用案であり、この計画段階ではブラウザ互換性や実機動作を検証していない。採用するAPIの現行仕様は実装時に公式資料で確認する。

## 開発ステップ

| 段階 | 目的・実装内容 | 完了の目安 |
| --- | --- | --- |
| 0. 設計を固める | 画面遷移、データ構造、日付の計算ルール、保存・復元形式を整理する。Git管理とファイル構成を準備する。 | 主要操作の流れとデータの持ち方が決まり、実装に着手できる。 |
| 1. 在庫管理の核を作る | IndexedDB、豆の追加・一覧・詳細・編集、古い順の表示、経過日数・年月、飲み終わり処理、履歴、確認付き完全削除。 | 登録から飲み終わりまで一通り操作でき、再読み込み後も状態が残る。 |
| 2. 日常利用とデータ保全を整える | プリセット管理、期間フィルター、JSON書き出し・全置換復元。余力があれば新しい順・豆名順も追加する。 | プリセットと自由入力の両方が使え、バックアップから在庫・履歴・プリセットを復元できる。 |
| 3. PWAと書き出しを完成させる | manifest、アイコン、Service Worker、オフラインキャッシュ、更新フロー、CSV書き出し、iPhone向けUI調整。 | ホーム画面起動とオフライン操作ができ、更新時も保存データを維持できる。 |
| 4. 公開環境と実機で仕上げる | GitHub Pagesのサブパス配信を確認し、iPhone Safari・ホーム画面起動で一連の操作を検証する。READMEを整備する。 | 仕様書の完了条件20項目をすべて確認し、初期版を公開できる状態になる。 |

進め方は **段階0 → 1 → 2 → 3 → 4**。段階1は動作確認用、段階2以降を実データでの試用候補とする。PWAの最小構成は段階1で接続を試し、オフラインと更新の仕上げを段階3で行う。

## 画面と操作の設計

ナビゲーションは「在庫 / アーカイブ / 設定」の3項目にする。追加・編集フォームと豆の詳細は、それぞれの一覧から開く補助画面として扱う。

| 画面 | 内容と主な操作 |
| --- | --- |
| 在庫 | 現在の袋数、期間フィルター、古い順の一覧、豆の追加。豆名・焙煎度・焙煎日・経過年月・日数を表示する。 |
| 豆の詳細 | 登録情報と経過期間を表示し、編集・飲み終わり処理に進む。 |
| 追加・編集 | 豆名のプリセット選択または自由入力、焙煎度1〜5または自由入力、焙煎日を入力する。 |
| アーカイブ | 飲み終わった豆と飲み終わり日時を確認する。詳細から確認付きの完全削除を行う。 |
| 設定 | プリセット管理、JSONバックアップ・復元、CSV書き出し。 |

落ち着いた見た目にし、長期保管は色だけでなく経過期間の文字でも伝える。空の一覧、保存中、入力エラー、保存失敗も設計に含める。

## 実装前に揃えるルール

以下は仕様書の曖昧な部分を埋める推奨初期案。実装開始時にこの案を基準とし、希望に応じて調整する。

| 論点 | 推奨初期案 |
| --- | --- |
| 1件の単位 | 1件＝1袋。同じ豆名・同じ焙煎日の袋も別IDで登録可能にする。数量・残量は扱わない。 |
| 日数計算 | 焙煎日は時刻を持たない日付として保存する。端末の今日の日付との暦日差を取り、時刻・夏時間で日数がずれない計算にする。 |
| 年月の表示 | 満了した暦月を基準に「1年4か月」などと表示し、1か月未満は「1か月未満」とする。日数を併記し、月末・うるう日の境界も確認する。 |
| 日付の更新 | 起動時、画面に戻った時、起動したまま日付が変わった時に再計算する。経過日数はDBに保存しない。 |
| フィルター | 180日以上、365日以上、548日以上を固定の条件にする。対象は現在の在庫のみ。 |
| 一覧の初期順 | 起動時は必ず焙煎日の古い順。同日の並びも登録日時等で安定させる。 |
| アーカイブの経過日数 | 仕様の「現在日まで」に合わせ、表示・CSVのageDaysは出力時点の今日までとする。飲み終わり日時は別に保持する。 |
| プリセットの変更 | 登録済みの豆名は独立した文字列として保持し、プリセットの編集・削除で変化させない。 |
| 入力チェック | 豆名・焙煎日・焙煎度を必須にする。空白だけの名称・その他焙煎度、不正な日付、未来の焙煎日を拒否する。 |
| 誤って飲み終わりにした場合 | 在庫に戻す操作は追加候補とし、必須範囲には含めない。 |

## データとファイル構成

豆は仕様書の`bean`構造を基本とし、`status`で在庫とアーカイブを分ける。飲み終わりで別ストアへ移す必要はない。プリセットは独立したストアに保存する。

- 豆：`id`, `name`, `roastType`, `roastValue`, `roastCustom`, `roastDate`, `createdAt`, `status`, `finishedAt`
- プリセット：`id`, `name`
- DB：バージョン管理し、将来項目を追加しても既存データを移行できる構造にする。
- JSON：`schemaVersion`, `exportedAt`, `beans`, `presets`を含め、形式の判定と将来の移行に備える。

ファイルは責務に応じて分け、次を目安にする。実装時に必要以上の分割はしない。

```text
CoffeeStrage/
  index.html
  style.css
  app.js                 # 画面表示・操作
  db.js                  # IndexedDBへの読み書き
  dates.js               # 日数・年月計算
  backup.js              # JSON復元・書き出し、CSV出力
  manifest.webmanifest
  service-worker.js
  icons/
  tests/                 # 日付・復元など重要なロジックの検証
  README.md
  ROADMAP.md
```

GitHub Pagesのリポジトリ配下で動くよう、アセット、manifest、Service WorkerのURLとスコープを設計する。画面切替は静的ホスティングで再読み込みできる方式にする。検証用ツールを導入する場合も、配信ファイルのビルドは必須にしない。

## バックアップと更新の設計

端末内保存を前提に、JSONを端末外へ持ち出せることをデータ保全の中心にする。JSONは復元用、CSVは閲覧用と役割を分ける。

復元は「ファイル読込 → 全体検証 → 置換対象の件数表示・確認 → 一括反映」の順にする。形式、必須項目、値、ID重複を確認し、不正ファイルでは現在データを変更しない。全置換はトランザクションで処理し、失敗時にデータが途中まで消えた状態を残さない。置換前に既存データを書き出せる導線を設ける。

CSVは日本語、カンマ、改行、引用符を含む豆名を正しく扱い、Excelでの文字化けと数式としての誤解釈も考慮する。

PWA更新ではアプリ本体のキャッシュと利用者のDBを分ける。更新検知時は保存操作を妨げず、新版を適用するタイミングを示す。公開先URLを変更するときはデータ移行への影響を確認する。

## 検証と初期版の完了条件

各段階で必要な検証を行い、最後に仕様書第23章の20項目を照合する。

| 分野 | 確認内容 |
| --- | --- |
| データ操作 | 追加・編集・飲み終わり・完全削除・プリセット変更が再読み込み後も保持される。保存失敗を成功として表示しない。 |
| 日付 | 当日、月末、年越し、うるう日、180・365・548日前の境界で計算とフィルターが一致する。未来日を拒否する。 |
| バックアップ | 書き出し→全置換復元で全項目が一致する。空データ、不正JSON、未知の形式、重複ID、処理失敗で既存データを壊さない。 |
| CSV | 在庫とアーカイブが入り、日本語や特殊文字が正しく読める。 |
| PWA | 初回読込後のオフライン起動・主要操作、Service Worker登録と更新、更新後のDB保持を確認する。 |
| 公開環境 | GitHub Pagesのサブパス上でアセット読込、再読み込み、manifest、キャッシュが動く。 |
| iPhone実機 | Safariとホーム画面起動の両方で、日付入力、キーボード表示、下部ナビ、safe area、JSON保存・読込を確認する。 |

自動検証は日付計算、入力検証、復元の整合性などに重点を置く。iPhoneでの操作感やファイル保存は実機で確認する。実機確認ができない場合は未確認項目を明記し、確認済みとは扱わない。

READMEには起動方法、GitHub Pagesの公開手順、ホーム画面への追加方法、オフライン利用の条件、バックアップ・復元手順を記載する。

## 次回の着手点

段階3としてCSV書き出し、PWAのmanifest・アイコン・Service Workerとオフライン動作を実装する。iPhone実機でのファイル保存・復元は公開環境の検証時に確認する。

## 優先順位の変更（2026-09-17）

使用量を抑えるため、ユーザーの指定によりCSV・オフラインキャッシュは後回しにする。manifest、ホーム画面アイコン、standalone表示とGitHub Pagesへの公開を先行する。Service Workerはまだ登録しないため、起動には通信が必要。iPhone実機でのホーム画面起動・ファイル保存と復元は未確認。
